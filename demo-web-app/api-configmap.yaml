apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
data:
  NODE_ENV: development
  PORT: 3001
  DATABASE_HOST: localhost
  DATABASE_PORT: 5432
  DATABASE_USERNAME: postgres
  DATABASE_PASSWORD: root
  DATABASE_NAME: admindb
  DATABASE_URL: postgresql://root:app123@localhost:5432/admindb?schema=public
