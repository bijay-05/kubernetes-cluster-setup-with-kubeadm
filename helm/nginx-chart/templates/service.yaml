apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookreview.fullname" . }}
  labels:
    {{- include "bookreview.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookreview.selectorLabels" . | nindent 4 }}
