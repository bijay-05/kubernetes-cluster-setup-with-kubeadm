apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bookreview.fullname" . }}-test-connection"
  labels:
    {{- include "bookreview.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bookreview.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
